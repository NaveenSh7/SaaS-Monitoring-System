# Development Dockerfile for Server-2 with hot-reload
FROM node:20-alpine

WORKDIR /app

# Install nodemon globally for hot reload
RUN npm install -g nodemon

# Copy package files
COPY package*.json ./
RUN npm install

# Copy application code
COPY . .

EXPOSE 5001

# Development mode uses nodemon for hot reload
CMD ["npm", "run", "dev"]
